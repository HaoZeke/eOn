The camelCase matches in `_options.` are all legitimate — they're members like `finiteDifference`, `conFilename`, `randomSeed` etc. that are camelCase in the struct definition in Parameters.h. Those are correct as-is.

All the build errors you reported are now fixed:

- **BasinHoppingJob.cpp**: `displacementAlgorithm` → `displacement_algorithm`, `displacementDistribution` → `displacement_distribution`
- **GlobalOptimizationJob.cpp**: `displacementDistribution` → `displacement_distribution`
- **MinimizationJob.cpp**: `params->writeMovies` → `params->debug_options.write_movies`
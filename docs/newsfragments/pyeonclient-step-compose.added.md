Public step composition for Matter-first NEB/min: ``write_neb_results``,
``pot_registry_total_force_calls`` at package root; ``NEB.find_extrema``;
GIL release on ``forces_free`` / ``max_force``. NebSpec covers EW/CI/OCI-MMF.

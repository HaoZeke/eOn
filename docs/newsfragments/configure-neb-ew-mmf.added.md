Add ``configure_neb_ew_mmf`` to set energy-weighted CI-NEB + OCI-MMF knobs on
``Parameters`` for class-first ``make_backend`` cookbook paths (same defaults
as the atomistic-cookbook ``eon-pet-neb`` tutorial).

QSC energy and force reuse pair-parameter caches correctly: parameter
structs are no longer zero-initialized on every neighbor evaluation, so
same-Z neighbor hits keep the last lookup instead of applying zeros.

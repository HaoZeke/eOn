Add ``eon_schema.config`` INI helpers (``write_ini``, ``hydrate_ini``,
``unknown_ini_keys``, ``write_models_ini``) so tooling can author validated
``config.ini`` from L0/L1 without importing eon-akmc.

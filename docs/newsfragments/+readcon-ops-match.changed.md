Structure matching and rigid alignment now live in the readcon-ops package at lode-org/readcon-ops. ``eon.atoms`` re-exports them. Parsing and writing stay in readcon, and the corpus stays in readcon-db.

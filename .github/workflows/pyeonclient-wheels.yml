# Standalone PyPI package: pyeonclient
# Artifacts (3 wheels):
#   1. abi3          — GIL CPython 3.12+ (built on 3.12, Limited API)
#   2. cp313t        — free-threaded CPython 3.13
#   3. cp314t        — free-threaded CPython 3.14
#
# Publish: tag pyeonclient-v* or workflow_dispatch with publish=true
# Requires PyPI Trusted Publisher for project "pyeonclient" (environment: pypi-pyeonclient).
name: pyeonclient wheels
concurrency:
  group: pyeonclient-wheels-${{ github.ref }}
  cancel-in-progress: true
on:
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish built wheels to PyPI"
        type: boolean
        default: false
  push:
    tags:
      - "pyeonclient-v*"
  pull_request:
    paths:
      - "client/python/**"
      - "client/**/*.cpp"
      - "client/**/*.h"
      - "pyproject-pyeonclient.toml"
      - ".github/workflows/pyeonclient-wheels.yml"
      - "scripts/pyeonclient_*.sh"
      - "subprojects/readcon-core.wrap"
      - "meson.build"
      - "meson_options.txt"

permissions:
  contents: read

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Stable ABI (one wheel for all GIL CPython >= 3.12) → cp312-abi3-manylinux_*
          - name: abi3-manylinux
            os: ubuntu-22.04
            python: "3.12"
            free_threaded: false
            cibw_build: "cp312-manylinux_x86_64"
            allow_limited_api: true
          - name: cp313t-manylinux
            os: ubuntu-22.04
            python: "3.13"
            free_threaded: true
            cibw_build: "cp313t-manylinux_x86_64"
            # free-threaded: no stable ABI; disable package limited-api default
            allow_limited_api: false
          - name: cp314t-manylinux
            os: ubuntu-22.04
            python: "3.14"
            free_threaded: true
            cibw_build: "cp314t-manylinux_x86_64"
            allow_limited_api: false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: false

      - name: Activate pyeonclient pyproject
        run: bash scripts/pyeonclient_prepare_pyproject.sh

      - name: Install host build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential gfortran cmake pkg-config \
            libeigen3-dev ninja-build \
            python3-pip
          # cbindgen for readcon-core wrap
          cargo install cbindgen 2>/dev/null || \
            (curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && \
             . "$HOME/.cargo/env" && cargo install cbindgen)
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - uses: pypa/cibuildwheel@v2.23.3
        env:
          CIBW_BUILD: ${{ matrix.cibw_build }}
          CIBW_BUILD_FRONTEND: build
          CIBW_SKIP: "pp* *-win32 *-manylinux_i686 *-musllinux*"
          CIBW_ARCHS: native
          # Free-threaded: enable when building *t tags
          CIBW_ENABLE: ${{ matrix.free_threaded && 'cpython-freethreading' || '' }}
          # Free-threaded builds must not claim abi3 (pyproject limited-api=true).
          CIBW_CONFIG_SETTINGS: ${{ matrix.free_threaded && 'setup-args=-Dpython.allow_limited_api=false' || '' }}
          # Pin cargo under /root in manylinux; quill installed under /usr/local in before-all.
          CIBW_ENVIRONMENT: >-
            PATH=/root/.cargo/bin:$PATH
            CARGO_HOME=/root/.cargo
            PKG_CONFIG_PATH=/usr/local/lib64/pkgconfig:/usr/local/lib/pkgconfig:$PKG_CONFIG_PATH
            CMAKE_PREFIX_PATH=/usr/local
          CIBW_BEFORE_ALL_LINUX: |
            yum install -y eigen3-devel gcc-gfortran || true
            export CARGO_HOME=/root/.cargo
            export PATH="/root/.cargo/bin:$PATH"
            command -v rustc || curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            . /root/.cargo/env
            command -v cbindgen || cargo install cbindgen
            if ! pkg-config --exists quill 2>/dev/null; then
              rm -rf /tmp/quill-src /tmp/quill-build
              curl -fsSL -o /tmp/quill.tgz https://github.com/odygrd/quill/archive/refs/tags/v11.0.2.tar.gz
              mkdir -p /tmp/quill-src && tar -xzf /tmp/quill.tgz -C /tmp/quill-src --strip-components=1
              cmake -S /tmp/quill-src -B /tmp/quill-build \
                -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_INSTALL_PREFIX=/usr/local \
                -DQUILL_BUILD_TESTS=OFF \
                -DQUILL_BUILD_EXAMPLES=OFF
              cmake --build /tmp/quill-build -j$(nproc)
              cmake --install /tmp/quill-build
            fi
          CIBW_BEFORE_BUILD: |
            pip install -U pip nanobind numpy meson ninja meson-python
          CIBW_TEST_REQUIRES: pytest numpy
          CIBW_TEST_COMMAND: python -c "import pyeonclient as p; print(p.__version__, p.Matter)"
          # GIL matrix row: limited-api=true → cp312-abi3-manylinux_*; freethreaded rows disable it.

      - uses: actions/upload-artifact@v4
        with:
          name: pyeonclient-${{ matrix.name }}
          path: wheelhouse/*.whl
          if-no-files-found: error

  # Optional pure sdist (source; users need compilers)
  sdist:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Activate pyeonclient pyproject
        run: bash scripts/pyeonclient_prepare_pyproject.sh
      - name: Host deps for meson-python sdist
        run: |
          sudo apt-get update
          sudo apt-get install -y libeigen3-dev gfortran ninja-build
          cargo install cbindgen 2>/dev/null || true
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - run: |
          python -m pip install -U pip build nanobind numpy meson ninja meson-python
          python -m build --sdist
          ls -la dist/
      - uses: actions/upload-artifact@v4
        with:
          name: pyeonclient-sdist
          path: dist/*.tar.gz

  publish:
    needs: [build, sdist]
    if: >-
      github.event_name == 'push' && startsWith(github.ref, 'refs/tags/pyeonclient-v')
      || (github.event_name == 'workflow_dispatch' && inputs.publish)
    runs-on: ubuntu-latest
    environment:
      name: pypi-pyeonclient
      url: https://pypi.org/p/pyeonclient
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Flatten artifacts
        run: |
          mkdir -p upload
          find dist -type f \( -name '*.whl' -o -name '*.tar.gz' \) -exec cp -v {} upload/ \;
          ls -la upload/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: upload/
          skip-existing: true
          verbose: true
